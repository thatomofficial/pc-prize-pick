<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\PcPrizePick.Application\PcPrizePick.Application.csproj" />
    <ProjectReference Include="..\PcPrizePick.Domain\PcPrizePick.Domain.csproj" />
    <ProjectReference Include="..\PcPrizePick.SharedKernel\PcPrizePick.SharedKernel.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      The DbContext + migrations live here, but they only need the EF Core
      runtime + Npgsql provider to compile. Microsoft.EntityFrameworkCore.Design
      (a design-time-only package) is referenced by the API startup project
      instead — dotnet ef requires the startup project to reference it.
    -->
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="AspNetCore.HealthChecks.NpgSql" />
  </ItemGroup>

</Project>
