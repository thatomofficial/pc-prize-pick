import { Environment } from '../app/_shared/models/environment.model';

export const environment: Environment = {
    production: false,
    apiEndpoint: 'https://localhost:7051/api',
    appName: 'PC Prize Pick (local default)'
};
